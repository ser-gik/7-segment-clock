


// Instantiate the module
led_display_driver instance_name (
    .data(data), 
    .digit_enable_mask(digit_enable_mask), 
    .display_led_segments(display_led_segments), 
    .display_led_enable_mask(display_led_enable_mask), 
    .reset(reset), 
    .clk(clk)
    );


