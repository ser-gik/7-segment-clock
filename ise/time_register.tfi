


// Instantiate the module
time_register instance_name (
    .time_bcd(time_bcd), 
    .time_to_load_bcd(time_to_load_bcd), 
    .clk_1Hz(clk_1Hz), 
    .load_new(load_new), 
    .increment_hours(increment_hours), 
    .increment_minutes(increment_minutes), 
    .increment_seconds(increment_seconds), 
    .will_wraparound_hours(will_wraparound_hours), 
    .will_wraparound_minutes(will_wraparound_minutes), 
    .will_wraparound_seconds(will_wraparound_seconds)
    );


